# @bugbundle/core

Core APIs and the versioned manifest schema for BugBundle.

```ts
import { captureCommand, inspectBundle, verifyBundle } from "@bugbundle/core";
```

The package is experimental. Schema version 1 is available from `@bugbundle/core/manifest.schema.json`.
